# 歌词管理系统配置文件
# 请根据需要修改以下配置

# 基本配置
PORT=3000
ADMIN_PASSWORD=admin
SESSION_SECRET=lyrics-admin-secret-key

# 环境配置
NODE_ENV=production

# 歌词目录路径配置
# 开发环境会使用相对路径: ../data/lyrics
# 生产环境路径 (可根据实际部署路径修改)
PRODUCTION_LYRICS_PATH=/root/data/plugins/astrbot_plugin_lyricnext/data/lyrics
